version: "3.8"
services:
  tests:
    build:
      context: .
      target: tests
    depends_on:
      - postgresql

  postgresql:
    image: postgres:13.0
    environment:
      POSTGRES_PASSWORD: "password"


  cli:
    build:
      context: .
      target: cli
    depends_on:
      - postgresql
